import Testing
@testable import Cocktails_4_0
import Foundation

/*
 final class MockCocktailService: CocktailService {
 var createdCocktails: [CocktailPayload] = []
 var updatedCocktails: [CocktailPayload] = []
 
 init() {
 // Calling super.init(imageService:) would be needed if it wasn't final or had required params
 // But CocktailService has an init(imageService:).
 // Wait, MockCocktailService needs a real or mock ImageService.
 // I'll just use a dummy one or override if possible.
 // Since CocktailService is final, I cannot inherit from it.
 // Let me check if CocktailService is final.
 }
 }
 */
